<div class="main">
  {{#each b}}
    <div class="picture">
      <img src="{{this.image_url}}" />
      <h2>{{this.name}}</h2>
      <h3>{{this.tagline}}</h3>
      <p>{{this.description}}</p>
    </div>
  {{/each}}
</div> 